import 'package:flutter/material.dart';

/// Data management UI has been removed.
/// This placeholder widget prevents import errors where the section was used.
class DataManagementSection extends StatelessWidget {
  const DataManagementSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // Return an empty container so settings layout remains unchanged.
    return const SizedBox.shrink();
  }
}
